server:
  port: 8080
  servlet.context-path: /start

spring:
  application:
    name: config-server
  profiles:
    active: dev

global:
  base-path: /home/srivatsan/config-server-data
  environment: kubernetes
